# Hosting links (project/org IDs) and local env files
.vercel/
dist/.vercel/
.env*
node_modules/
.DS_Store
Thumbs.db

# Local Claude Code settings
.claude/settings.local.json

# Local-only files written by database scripts (one-time invitation links)
database/.local/
